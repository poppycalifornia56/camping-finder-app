import React from "react";

function NavBar() {
  return (
    <header className="navbar">
      <h1>CampFinder</h1>
      <nav>
        <a href="/">Map</a>
        <a href="/">About</a>
        <a href="/">Contact</a>
      </nav>
    </header>
  );
}

export default NavBar;
